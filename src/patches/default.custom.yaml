# 全局配置补丁
#
# 5.0 起只剩两个方案：中文九键不再是独立方案。
# 九宫格与 26 键共用 rime_ice，见 rime_ice.custom.yaml 末尾「九宫格并入本方案」。
patch:
  # 方案列表，顺序即方案切换的循环顺序
  schema_list:
    - {schema: rime_ice}   # 全拼 —— 26 键中英混打 + 九宫格，共用此方案
    - {schema: melt_eng}   # 纯英文键盘（有词库、补全、错拼容错）
